## 角色、场景与连续性锁定

- 源片：17.033 秒，1080×1920，30 fps；保留 9:16 竖屏、湖泊/枯木/阴天冷灰蓝自然光、原始镜头推进节奏、黑色无袖层叠纱裙、人物站位、手势、口型节奏与所有非替换构图。
- 角色替换：仅湖边真人段的女性身份替换为参考图中的成年深肤色女性；锁定她的椭圆脸、长款深棕黑色大波浪卷发、柔和微笑、肤色与五官辨识度。服装仍严格沿用源片黑色无袖纱裙。
- 演唱者：`CHARACTER_A`，即参考图女性；两段均为唯一、已确认的在镜演唱者。除其外任何人不得唱歌、说话或做口型。
- UI 段：以用户提供的 `7月16日(1).mp4` 原样不透明拼接，禁止 OCR、重绘、文案改写、变速、循环、冻结或送入图生视频。
- 尾部段：以用户提供的 `7月16日.mp4` 原样替换源片端卡，禁止 OCR、重绘、文案改写、变速、循环、冻结或送入图生视频；尾部以该素材最后一个有效画面结束。
- 音乐：整支成片从输出 00:00.000 对齐使用用户上传《修炼爱情》原音频 01:30.000 开始处；生成真人段把对应片段作为 `@Audio1` 上传并用于演唱口型，UI/尾部由最终混音以该歌曲为主声轨覆盖。
- 歌词确认状态：本次本机 Whisper 小模型对音乐转写仍有替字/漏字，以下为需逐字确认的候选，尚不可提交 Seedance。确认后，候选文字将作为唯一的 `exact_sung_text` 冻结；不得改写、补词或让模型自行生成歌词。
- 文字：源片首段 `You Say` 与后段英文宣传句改为确定性文字层：首段 `修炼爱情`；后段 `想象一下，唱出你最爱的旋律`。位置、白字层级、出现/消失时间与源片一致；不得让模型生成文字。

## 逐镜反解

| Cut | 输出时间 | 源片时间 | 画面、动作与镜头锁定 | 路由 / 演唱合同 |
| --- | --- | --- | --- | --- |
| C01 | 00:00.000–00:05.000 | 00:00.000–00:05.000 | 湖边黑裙女性立于枯木旁，面向镜头微笑；镜头由中全景平滑推进至中景。替换为参考图女性，保留黑裙、姿态、环境、光线和推进路径。首段确定性文字 `修炼爱情` 继承源片 `You Say` 的位置与时间窗。 | 生成段 S01；上传 `@Audio1`=《修炼爱情》01:30.000–01:35.000。`CHARACTER_A` 在 00:00.000–00:05.000 面向镜头逐字唱出候选歌词：`几年前的幻想，几年后的原谅；为一张脸去养一身伤`。口型必须与此时间窗和 `@Audio1` 同步。**请逐字确认/修正该候选。** |
| C02 | 00:05.000–约00:09.713 | 00:05.000–00:10.000 | 原 UI 区间整段移除，拼接 `7月16日(1).mp4` 的有效画面与原始内部动画；入口保持源片硬切节奏。 | 不透明 UI 拼接；主声轨继续《修炼爱情》；不要求任何人物演唱。 |
| C03 | 约00:09.713–约00:14.713 | 00:10.000–00:15.000 | 回到同一湖边与枯木，替换为同一参考图女性；保留黑色纱裙、轻微头部/身体运动及原镜头构图。后段确定性文字 `想象一下，唱出你最爱的旋律` 继承源片英文句的位置、两行层级与时间窗。 | 生成段 S02；上传 `@Audio1`=与输出连续主声轨一致的对应歌曲窗口。`CHARACTER_A` 在 S02 00:00.000–约00:02.287 逐字唱出候选歌词：`我会受不了`；随后只保持自然收尾和呼吸，禁止补唱。口型必须与此时间窗和 `@Audio1` 同步。**请逐字确认/修正该候选。** |
| C04 | 约00:14.713–约00:19.426 | 00:15.000–00:17.033 | 删除源片 SingUp 端卡，以 `7月16日.mp4` 的有效画面完整替换；不得补黑、拉长、循环或冻结。 | 不透明尾部拼接；主声轨继续《修炼爱情》直至尾部最后有效帧；不要求任何人物演唱。 |
