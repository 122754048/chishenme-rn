# USFR Windows 部署包同步本地最新能力目标

## 目标状态

在不修改 `C:\Users\zhaocx04\.codex\skills\universal-source-fidelity-replication` 中任何文件、目录、内容或时间戳的前提下，以服务端已经配置完成的
`usfr-video-service-2026-07-29-deploy-windows.zip` 为唯一部署主干，完成本地 Skill 最新视频复刻能力的增量同步，并交付一个可独立部署、可验证、可回滚的新 Windows 服务包。

该目标持续有效，直到新部署包完成全部离线验证、公共 HTTP 验证、发布闭包验证、清理、重新打包和哈希校验为止。

## 不可变约束

1. 本地 Skill 只读，不得修改、删除、新增、移动或重命名其中任何文件。
2. 原部署 ZIP 只读，必须保留原 SHA-256 和独立回滚副本。
3. 服务端现有微软侧 GPT/OpenAI-compatible 接入、Redis、阿里云 OSS、公共 HTTP、鉴权、幂等和既有 bugfix 不得回退。
4. 真实 `.env` 不覆盖、不打印、不进入差异报告。
5. 禁止整目录或整文件树覆盖；冲突核心文件必须按函数、状态和合同增量合并。
6. 任何新行为必须先有失败测试，再写最小实现。
7. 离线验证全部通过前不得调用付费 Provider。
8. 生产包不得包含缓存、测试生成物、历史任务、历史媒体、真实 `.env` 或旧发布包。

## 必须同步的业务能力

- 只改语言快捷路线：翻译、TTS、口型同步、60 秒内 QC，不生成故事板和新视频。
- 非音乐语音识别：原视频直接上传指定语音识别应用，纯音乐路线跳过。
- 歌曲、正常口播、只改语言口播、旁白和背景音乐严格分轨。
- 歌曲与只改语言口播使用不同且不可互换的口型工作流。
- 上传歌曲不反推或展示歌词，音频字节与确认演唱区间是执行权威。
- 旁白使用原旁白音色作为 TTS 参考，并且不做人脸口型同步。
- 一张源帧总图一次生成一张替换控制总图，禁止逐 Cut 和本地换脸。
- 导演故事板运行时使用固定模板，每页最多四个 Cut、最多两页、整组一次确认。
- 视频生成前强制源视频身份隔离，新模特和新产品证据是目标权威。
- 用户可见内容不出现模型、供应商、工作流、节点或内部流程名称。
- 最终 QC 总墙钟时间不超过 60 秒。
- 路由优先、单次分析、缓存复用、安全并行和快速编码能力生效。

## 必须保留的服务端能力

- 三个公共业务 HTTP 路径及其简化入参、出参。
- job 级 Bearer token、token hash、`Idempotency-Key` 和稳定重放。
- `IMPORTING → import_sources → ANALYZING` 链路。
- Redis CAS、lease、ACK、失败原子提交、Provider 幂等和清理。
- URL 素材导入的 HTTPS、SSRF、重定向、大小、类型和时长检查。
- OSS 原素材与最终 MP4 永久保存、永久 URL 和 final 不删除策略。
- GPT 请求缓存、source evidence bundle、revision instruction 和服务端诊断。
- UI 重建默认关闭；UI 截图/商店链接继续分析产品；UI 操作视频继续剪辑替换。

## 验收条件

1. 本地 Skill 前后完整文件清单、SHA-256、大小和时间戳快照完全一致。
2. 原 ZIP 与回滚副本 SHA-256 完全一致。
3. 新部署包严格 verifier 通过。
4. 受保护的 GPT、Redis、OSS、HTTP、鉴权和幂等回归全部通过。
5. 新同步的语言、声音、故事板、身份隔离、内容屏蔽、QC 和性能定向测试全部通过。
6. `docker compose config` 和无付费公共 HTTP smoke 通过。
7. 新包不包含缓存、历史生成物、测试素材、真实 `.env` 或临时审计文件。
8. 新 ZIP、manifest 和 SHA-256 一一对应。
9. 提供中文增量更新、配置检查、上线和回滚手册。
10. 原部署 ZIP 和本地 Skill 均保持不变。

## 最终交付

- 新部署 ZIP。
- 新部署 ZIP 的 SHA-256 文件。
- 差异与验证报告。
- 中文服务端增量更新与回滚手册。
- 本地 Skill 未修改证明。

